{
  "name": "card-transaction-3ds",
  "version": "1.0.0",
  "description": "Enterprise 3DS Card Transaction Visualizer - DatMan QA Assignment",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev:client": "cd client && npm run dev",
    "dev:api": "node api/index.js",
    "install:all": "npm install && cd client && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.2",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
